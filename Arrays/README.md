# Arrays - DSA Notes
command  for deleting  find . -name .DS_Store -delete
To delete some folders
  - git rm -r Strings
  git status should be green
  git commit -m "Remove Strings folder"
  git push origin main

  arr[x] = oldValue + (newValue * n)
